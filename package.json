{
  "name": "health-safety-checklist",
  "version": "1.0.0",
  "description": "NJ Department of Education Health and Safety Evaluation of School Buildings Checklist",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "npm run dev --workspace=server",
    "dev:client": "npm run dev --workspace=client",
    "build": "cd client && npm run build && cd ../server && npm run build",
    "build:all": "cd client && npm run build && cd ../server && npm run build",
    "start": "node server/dist/server.js",
    "start:production": "node server/dist/server.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
